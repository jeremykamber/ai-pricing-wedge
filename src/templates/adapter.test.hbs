import { describe, it, expect } from 'vitest'
import { {{pascalCase name}}Impl } from '../{{pascalCase name}}Impl'

describe('{{pascalCase name}}Impl', () => {
  it('should be defined', () => {
    expect({{pascalCase name}}Impl).toBeDefined()
  })
  // Add more tests here
})
