import { describe, it, expect } from 'vitest'
import { dbTo{{pascalCase name}}, {{camelCase name}}ToDb } from '../{{pascalCase name}}Mapper'

describe('{{pascalCase name}}Mapper', () => {
  it('should map db record to entity', () => {
    const db = { id: '1' }
    const entity = dbTo{{pascalCase name}}(db)
    expect(entity.id).toBe('1')
  })
  it('should map entity to db record', () => {
    const entity = { id: '1' }
    const db = {{camelCase name}}ToDb(entity)
    expect(db.id).toBe('1')
  })
  // Add more tests here
})
